---
---

<section>
  <div>
    <img src="/icons/icon-search.svg" alt="" />

    <input type="text" name="" placeholder="Search Github username..." />
  </div>

  <div>
    <!-- <span>No results</span> -->

    <button type="submit">Search</button>
  </div>
</section>

<style scoped>
  section {
    display: flex;
    flex-direction: row;
    align-items: center;
    /* gap: 1rem; */
    justify-content: space-between;
    background: var(--dark-blue2);
    padding: 1rem 0;
    border-radius: 1rem;
    width: 100%;
  }

  span {
    display: block;
    font-size: 13px;
    color: var(--red);
  }

  div {
    display: flex;
    flex-direction: row;
    align-items: center;
    padding: 0 1rem;
  }

  input {
    all: unset;
    border: none;
    font-size: 14px;
    margin: 0 1rem;
    width: auto;
    min-width: 22vw;
    background: var(--dark-blue1);
    color: var(--white);
  }

  button {
    border: none;
    border-radius: 0.5rem;
    padding: 0.8rem 1.3rem;
    margin: 0 1rem;
    background: var(--dark-blue);
    color: var(--white);
    font-weight: 600;
    cursor: pointer;
  }
</style>
